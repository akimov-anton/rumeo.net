{{#link-to 'video' video.id}}
    <img src="{{video-image video.source}}">
    {{#if video.title}}
        <span class="video_title">
            {{video.title}}
        </span>
    {{/if}}
{{/link-to}}
